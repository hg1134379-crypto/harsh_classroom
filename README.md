# harsh_classroom
The project immediately establishes a solid grasp of Object-Oriented Programming (OOP). The dedicated model classes, User.java and Course.java, exemplify rigorous encapsulation by providing controlled access to their state via getters and setters
